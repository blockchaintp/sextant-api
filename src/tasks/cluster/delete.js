const Promise = require('bluebird')

const ClusterDelete = ({
  
}) => function* clusterCreateTask(params) {

  const {
    store,
    task,
    trx,
  } = params

  
}

module.exports = ClusterDelete